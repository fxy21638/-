# 基于频域特征与源-滤波器模型的实时语音性别识别系统

## —— 信号与系统项目报告

---

**摘要**：本项目设计并实现了一个实时语音性别识别与变声系统。系统以 22kHz 采样率采集音频，基于短时傅里叶变换 (STFT) 提取 18 维频域声学特征，使用 XGBoost 分类器进行男/女二分类，演员独立划分下测试集准确率 79.2%。变声模块基于 WORLD 声码器实现源-滤波器模型的完整分析-修改-合成闭环，创新性地采用基于共振峰检测的局部频域拉伸替代传统全局 warping。本项目覆盖了采样定理、傅里叶变换、滤波与卷积、统计信号处理、源-滤波器模型等信号与系统课程的核心知识点。

**关键词**：STFT；频域特征；基频估计；源-滤波器模型；WORLD 声码器；共振峰移位；XGBoost

---

## 一、引言

语音信号是人类最自然的交流媒介，其中蕴含着丰富的频域信息。男女声音的差异主要体现在基频（F0）和频谱包络（共振峰结构）上：成年男性基频通常为 85-180 Hz，女性为 165-255 Hz。这种频域差异为基于信号处理的性别识别提供了物理基础。

信号与系统课程教授了从时域到频域的桥梁——傅里叶变换，以及信号通过线性时不变（LTI）系统的分析方法。本项目将这些理论知识应用于一个完整的工程实践：从麦克风采集模拟信号、采样量化为离散序列、加窗分帧做 STFT 变换提取频域特征，到最终的分类决策与语音合成，形成了一个完整的"信号采集 → 域变换 → 特征分析 → 分类/合成"闭环。

---

## 二、系统总体架构

系统由两条管线组成：

- **识别管线**（每 0.8 秒触发）：麦克风 → 环形缓冲区 → 滑动窗口(3s) → STFT → 18 维频域特征 → XGBoost → 性别输出 + 男女声占比
- **变声管线**（用户触发）：环形缓冲区 → WORLD 分析(F0+SP+AP) → F0缩放 + 共振峰移位 + 频谱整形 → WORLD 合成 → 变声输出

![系统架构](report_figures/fig10_architecture.png)

**多速率实时处理设计**：系统采用三速率并行架构——音频采集 22kHz，图表刷新 10Hz（100ms 定时器），模型预测 ~1.25Hz（0.8s 定时器）。高速图表定时器从缓冲区取 0.4s 音频更新幅度和频谱显示，预测定时器取 3s 窗口音频送入 XGBoost 推理，两者独立运行、互不阻塞。

---

## 三、采样定理与离散时间信号处理

### 3.1 采样率选择与 Nyquist 定理

Nyquist 采样定理指出：要从采样后的离散序列无失真地重建原始连续信号，采样频率 $f_s$ 必须大于信号最高频率 $f_{max}$ 的两倍，即 $f_s > 2f_{max}$。

语音信号的有效带宽约为 8-11 kHz（人类听觉上限约 20 kHz，但语音能量主要集中在中低频）。本系统选择 $f_s = 22050$ Hz，满足 $22050 > 2 \times 11025$ Hz，能够无失真地保留语音信号的全部频域信息。

```python
# voice_gender_gui.py
SAMPLE_RATE = 22050
```

### 3.2 频谱特征的 Nyquist 归一化

所有频谱相关特征的计算结果依赖于具体的采样率。为了使特征值与采样率无关，系统将所有频率域特征除以奈奎斯特频率 ($f_s/2$) 做归一化，约束到 $[0, 1]$ 区间：

$$\text{meanfreq} = \frac{\text{spectral\_centroid}}{f_s/2}, \quad \text{sd} = \frac{\sigma(\text{centroid})}{f_s/2}$$

归一化后的特征具有跨采样率的可比性，这是信号处理中尺度不变性的具体应用。

### 3.3 块处理与实时分帧

系统以 2048 采样点为一个处理块，持续时间 $\Delta t = 2048 / 22050 \approx 93$ ms。这在概念上等同于离散时间信号处理中的"分帧"操作——将无限长序列分割为有限长度的帧，逐帧处理。

```python
sd.InputStream(samplerate=22050, blocksize=2048, callback=_audio_callback)
```

### 3.4 环形缓冲区——流式信号存储

![环形缓冲区](report_figures/fig7_ringbuffer.png)

实时音频流无法预先知道总长度，传统数组的线性增长会导致内存耗尽。本系统使用固定容量的循环缓冲区（容量 15 秒 $\times$ 22050 Hz = 330,750 样本），写指针到达末尾后循环回到开头覆盖旧数据。这一设计实现了 O(1) 写入复杂度，同时支持 O(n) 滑动窗口提取。

```python
class AudioRingBuffer:
    def __init__(self, capacity_seconds=15.0, sr=22050):
        self._buffer = np.zeros(int(capacity_seconds * sr), dtype=np.float32)
        self._write_pos = 0  # 循环写指针

    def write(self, data):  # O(1)
        ...
        self._write_pos = (self._write_pos + n) % cap

    def get_last(self, duration_seconds):  # O(n), 提取最近N秒
        start = (self._write_pos - n_samples) % cap
        ...
```

### 3.5 滑动窗口提取

预测时从环形缓冲区取最近 3 秒数据（$3 \times 22050 = 66,150$ 样本），窗口以实时采集速率向前滑动。预测每 0.8 秒触发一次，即相邻预测窗口有约 73% 重叠，既保证预测的连续性又避免计算资源浪费。这与信号处理中短时分析的重叠分帧思想一致。

---

## 四、傅里叶变换与频域分析

### 4.1 STFT——从时域到频域的桥梁

傅里叶变换将时域信号映射到频域，揭示信号的频率成分。但语音是非平稳信号——频率成分随时间变化（不同音节对应不同频谱）。因此不能对整段语音做一个 DFT，而需要对加窗分帧后的每一帧分别做 DFT，这就是短时傅里叶变换 (Short-Time Fourier Transform, STFT)：

$$X(t, f) = \sum_{n=0}^{N-1} x[n + tH] \cdot w[n] \cdot e^{-j2\pi kn/N}$$

其中 $w[n]$ 为窗函数，$H$ 为帧移，$N$ 为 FFT 点数。

```python
# main.py
stft = np.abs(librosa.stft(y, n_fft=2048))
freqs = librosa.fft_frequencies(sr=sr, n_fft=2048)
```

STFT 将 1D 时域信号 $y(t)$ 映射为 2D 时频谱 $S(t, f)$。本项目的 18 个声学特征全部基于此变换——没有任何特征直接从时域采样值计算，体现了"频域分析"在信号处理中的核心地位。

![语谱图对比](report_figures/fig1_spectrograms.png)

**图1 分析**：从语谱图可观察到 — (a) 男声的基频谐波间距较窄（低频条纹密集），高频能量较弱（600 Hz 以上颜色偏暗）；(b) 女声的基频谐波间距较宽，高频谐波能量显著更强，尤其是在 1500-3500 Hz 范围。这种频域差异是所有后续特征提取和分类的物理基础。

### 4.2 FFT 共轭对称性

实信号的傅里叶变换具有共轭对称性：$X[-k] = X^*[k]$。因此 DFT 的负频率部分包含的信息与正频率完全冗余。本系统只存储正频率部分（含 DC 分量和 Nyquist 频率），即 $n_{fft}/2 + 1 = 1025$ 个频点。

### 4.3 频谱质心——频率域一阶矩

频谱质心是频谱的"重心"，定义为频率的幅度加权平均：

$$\text{centroid} = \frac{\sum_{f} f \cdot |S(t,f)|}{\sum_{f} |S(t,f)|}$$

它反映了声音的"明亮度"——女声高频能量更丰富，质心偏高；男声低频能量占主导，质心偏低。这是性别分类中最直观的频域特征之一。

### 4.4 谱熵——频域信息度量

将每帧功率谱归一化为概率分布 $P(f) = |S(f)|^2 / \sum_f |S(f)|^2$，计算 Shannon 熵：

$$H = -\sum_{f} P(f) \cdot \log_2 P(f)$$

物理意义：白噪声的功率在各频率均匀分布 $\rightarrow$ 高熵；纯音的功率高度集中 $\rightarrow$ 低熵。用 $\log_2(n_{bins})$ 做最大熵归一化，将值域约束到 $[0, 1]$。

### 4.5 频域对比分析

![频谱对比](report_figures/fig4_spectrum.png)

**图4 分析**：FFT 幅度谱揭示了男女声在频域的显著差异：(1) 男声基频约 100-150 Hz，谐波集中在 1 kHz 以下；(2) 女声基频约 200-250 Hz，谐波延伸至 3 kHz 以上；(3) 男声高频谐波衰减速度明显快于女声——这是谱倾斜 (Spectral Tilt) 的直接体现，也是变声模块中"频谱倾斜调整"的理论依据。

---

## 五、基频估计

### 5.1 YIN 算法——时域自相关法

YIN 算法基于自相关函数的时域周期检测，是自相关法基频估计的经典改进。其核心思想是：周期信号的自相关函数在滞后等于周期时出现峰值。YIN 通过累积均值归一化差分函数 (Cumulative Mean Normalized Difference Function) 抑制了自相关法的倍频/半频误检问题：

$$d_t(\tau) = \sum_{j=1}^{W} (x_j - x_{j+\tau})^2$$

$$d'_t(\tau) = \begin{cases} 1, & \tau = 0 \\ \frac{d_t(\tau)}{\frac{1}{\tau}\sum_{j=1}^{\tau} d_t(j)}, & \tau > 0 \end{cases}$$

基频 = $f_s / \tau_{min}$，其中 $\tau_{min}$ 为 $d'_t(\tau)$ 第一个低于阈值的谷点。本系统搜索范围设置为 50-500 Hz，覆盖人类语音 F0 的全部范围。为抑制清音帧的虚假 F0 值，对 YIN 输出施加 [50, 450] Hz 的硬性剪裁，剔除超过 450Hz 的伪迹。

```python
# main.py
f0 = librosa.yin(y, fmin=50, fmax=500, sr=sr)
f0 = f0[(f0 >= 50) & (f0 <= 450)]  # 剔除清音伪迹
```

![F0轮廓对比](report_figures/fig2_f0_contour.png)

**图2 分析**：(a) 男性基频主要在 80-180 Hz，变化平缓、波动小；(b) 女性基频主要在 170-350 Hz，且波动幅度和频率均大于男性。红色虚线标注的平均 F0 是区分性别的主导特征（占模型重要性的 ~10%）。值得注意的是 F0 并非恒定——句末基频下降（declination）和声调变化造成的起伏清晰可见。

### 5.2 Harvest 算法——频域多分辨率法

Harvest 是 pyworld 提供的更鲁棒的基频估计器，结合多分辨率频谱分析，对噪声和清音有更好的抗干扰能力。配合 StoneMask 去除清音帧的 F0 假值（清音/无声帧本质上没有基频）：

```python
f0, t = pw.harvest(y, sr, f0_floor=50, f0_ceil=500)
f0 = pw.stonemask(y, f0, t, sr)
```

### 5.3 F0 平滑——中值+插值两阶段处理

基频估计不可避免地带有野值（倍频/半频误检、清音段跳变）。系统采用**两阶段**组合策略进行修复：

（1）**中值滤波**（非线性去野）：对邻域内有声帧取中值，消除孤立野值的同时保留清/浊音边界
（2）**局部线性插值**（线性补洞）：对 $\leq 6$ 帧（~30ms）的短断裂桥接修复

```python
def _smooth_f0(f0, window=5):
    # 阶段1: 有声帧中值滤波去野值
    for idx in voiced_mask:
        smoothed[idx] = median(neighborhood[voiced])
    # 阶段2: 局部线性插值填空洞
    for gap <= 6 frames:
        smoothed[a:b+1] = linspace(smoothed[a], smoothed[b])
```

这种"中值（非线性去野）+ 插值（线性补洞）"的组合正是信号与系统课程中 LTI 滤波 + 非线性处理的工程实例。

---

## 六、统计信号处理

### 6.1 偏度与峰度——频谱形状的高阶矩

频谱质心的时间序列反映了声音"明亮度"的动态变化。对其做高阶统计量分析：

**偏度**（三阶标准化矩）衡量分布的不对称性：
$$\text{skew} = E\left[\left(\frac{X-\mu}{\sigma}\right)^3\right]$$

正值表示质心分布右偏（偶发高频尖峰），负值表示左偏（低频能量持续偏高）。

**峰度**（四阶标准化矩）衡量分布的"尖峰程度"：
$$\text{kurt} = E\left[\left(\frac{X-\mu}{\sigma}\right)^4\right]$$

高值表示能量集中在少数频段（如单一元音持续），低值表示能量在频域分散（如摩擦音、爆破音）。

### 6.2 IQR 与分位数

四分位距 $\text{IQR} = Q_{75} - Q_{25}$ 是频谱质心的鲁棒散布度量，相比标准差对野值不敏感。$Q_{25}$ 和 $Q_{75}$ 分别代表声音"最暗的 25%"和"最亮的 25%"的频域位置。

### 6.3 调制指数——变异系数

$$\text{modindx} = \frac{\sigma(\text{F0})}{\mu(\text{F0})}$$

F0 的标准差与均值之比，衡量音高变化的相对幅度。女性在交流中通常使用更大的音高变化范围，因此 modindx 是女性话语的统计特征之一。

![特征箱线图](report_figures/fig8_feature_boxplot.png)

**图8 分析**：箱线图直观展示了男女声在关键特征上的分布差异 — (1) meanfreq（频谱质心）男声显著低于女声，箱体中位数差值明显；(2) meanfun（平均基频）是区分度最高的单一特征；(3) IQR 和 sd（质心标准差）女声略高，反映女性声音的频谱变化性更强；(4) meandom（平均主频）呈现与 meanfun 相似的趋势。

---

## 七、源-滤波器模型与 WORLD 变声

### 7.1 语音产生的物理学模型

语音信号处理中，语音的产生被建模为**源-滤波器模型** (Source-Filter Model)：

$$S(z) = E(z) \cdot H(z)$$

- **源 $E(z)$**：声带振动产生的声门脉冲（准周期信号），决定基频和清/浊音类型
- **滤波器 $H(z)$**：声道（口腔、鼻腔、咽腔）的共振特性，决定共振峰位置和音色

WORLD 声码器将这一模型参数化为三个可独立修改的分量，实现分析-修改-合成的完整闭环。

### 7.2 WORLD 分析——三参数提取

```python
f0, t = pw.harvest(y, sr, f0_floor=50, f0_ceil=500)   # ① 基频 (源)
sp = pw.cheaptrick(y, f0, t, sr)                        # ② 频谱包络 (滤波器)
ap = pw.d4c(y, f0, t, sr)                               # ③ 非周期性 (谐波/噪声)
```

| 参数 | 物理含义 | 源-滤波器对应 | 提取算法 |
|------|---------|-------------|---------|
| F0 | 声带振动频率 | 源激励 $E(z)$ | Harvest + StoneMask |
| SP | 声道共振特性 | 滤波器 $H(z)$ | CheapTrick |
| AP | 各频段噪声占比 | 激励类型 | D4C |

### 7.3 参数修改——频域操作

**F0 缩放**（改变音高）：
$$\text{F0}_{new} = \text{F0} \times \text{f0\_ratio}$$

$\text{f0\_ratio}$ 由目标性别和当前基频均值决定。男→女时目标基频 $= 200 + 60 \times strength$ Hz，比上限 $= 1.80 + 0.80 \times strength$（默认强度 0.55 时上限 2.24）；女→男时目标基频 $= 120 - 55 \times strength$ Hz，比下限 $= 0.65 - 0.40 \times strength$。

**共振峰移位**（改变音色/元音品质）——基于共振峰检测的局部频域拉伸：

传统方法对整个频谱包络做均匀频率轴拉伸（全局 warping），会同时拉伸非共振峰区域的噪声和伪影。本项目创新性地采用**局部共振峰移位**策略：

1. `_estimate_formant_peaks()` — 在 log 域用 `scipy.signal.argrelextrema` 检测每帧频谱包络的 F1-F4 共振峰
2. 共振峰频段约束：F1(200-900Hz), F2(800-2400Hz), F3(2000-3500Hz), F4(3000-4800Hz)
3. 对每个共振峰的 ±30% 频宽区域做局部频率轴拉伸（`np.interp` 重采样）
4. 混合比 $0.50 + 0.25 \times strength$（男→女移位 18-33%，女→男移位 10-20%）
5. 若 <10% 帧检出共振峰（极端非语音信号），回退到全局 warping

这种方法相比全局 warping 的优势在于：仅修改共振峰区域（声道滤波器的关键特征），保留其余频段的自然结构。

**频谱倾斜**（改变明亮度）——多段非线性频率域滤波器：

男→女时：大幅削减 50-550Hz 低频（去除男声厚度），1.5-3.2kHz 中高频微调避免主导频率（meandom）和频谱质心（centroid）超出训练数据分布边界。

女→男时：大幅增强 50-250Hz 低频（增加男声厚度），大幅削减 1.5kHz 以上高频。

**频谱混合**——频率自适应的原声/变声融合：
$$\text{mix\_ratio}(f) = \text{linspace}(0.85, 0.91, n_{bins}) + strength \times 0.06 + 性别偏移$$

混合比为 85-91% 转换谱 + 9-15% 原始谱，展平的 linspace 使各频段混合一致，避免高低频段处理差异过大。

### 7.4 非周期性调整

非周期性参数反映了各频段中噪声能量占总能量的比例。女性声音的高频非周期性更高（气息感），男性声音更趋于完全谐波。系统根据转换强度动态调整：

$$\text{ap\_adj}(f) = \text{ap}(f) + \text{adjustment}(f) \times |\text{f0\_ratio} - 1.0|$$

调整为逐频点操作，高频调整量大于低频，且仅在 F0 > 0 的浊音帧上执行（清音帧保持原样）。调整量与转换强度成比例，clip 到 $[0, 1]$。

### 7.5 WORLD 合成——从参数到波形

```python
y_converted = pw.synthesize(f0_converted, sp_processed, ap_adjusted, sr)
```

从修改后的 F0（源）+ 频谱包络（滤波器）+ 非周期性（噪声配比）三个参数重建时域波形，完成"分析 → 修改 → 合成"的完整闭环。

![WORLD管线](report_figures/fig9_world_pipeline.png)

**图9 分析**：此图展示了 WORLD 变声管线的完整流程——从原始 F0 的缩放和平滑，到频谱包络的共振峰移位和倾斜，再到非周期性调整和最终合成。每个步骤对应源-滤波器模型中的一个操作，可以看到 F0 从约 250 Hz（女声）降低到约 150 Hz（男声），频谱包络的高频能量减弱（女→男的典型变化）。

---

## 八、滤波与信号平滑

### 8.1 频谱包络平滑——同态滤波

频谱包络估计中不可避免地存在快起伏噪声和帧间不连续。Web 版变声管线采用同态滤波进行频率维平滑：

$$SP_{smooth}[k] = \frac{1}{N}\sum_{i} \log(SP[k+i]), \quad SP = \exp(SP_{smooth})$$

取 log → 卷积 → exp 的过程称为**同态滤波**，能有效消除频谱的快起伏（本质上对应于声道脉冲响应的"纹波"），同时保留宽的共振峰结构。

> 注：桌面 GUI 变声管线经测试发现，同态滤波平滑会降低频谱熵（sp.ent），使模型更倾向于判断为男声。因此 GUI 版已移除此步骤，采用更直接的共振峰移位 + 混合方式。

### 8.2 VAD 人声活动检测——能量域阈值判决

![VAD演示](report_figures/fig6_vad_demo.png)

系统将音频按 100ms 分帧，计算每帧 RMS 能量：

$$RMS_{frame} = \sqrt{\frac{1}{N}\sum_{n=1}^{N} y[n]^2}$$

采用自适应阈值机制：以第 20 百分位作为噪声基底估计，阈值取 $\max(noise\_floor \times 3,\ 0.0015)$。若 RMS 超过阈值的帧占比不足 10%，判定为无效语音。

```python
noise_floor = np.percentile(frame_rms, 20)
vad_threshold = max(noise_floor * 3.0, 0.0015)
active_ratio = np.mean(frame_rms > vad_threshold)
```

自适应阈值的优势在于：不同环境、不同麦克风的底噪水平不同，固定阈值无法通用。以噪声基底为参考的动态阈值是信号检测理论（恒虚警率 CFAR 检测）在音频领域的简化和应用。

---

## 九、频域特征空间与分类

### 9.1 全部 18 个特征来源于频域

| 序号 | 特征名 | 物理含义 | 域 |
|------|--------|---------|-----|
| 1 | meanfreq | 平均频谱质心 | 频域 |
| 2 | sd | 频谱质心标准差 | 频域 |
| 3 | median | 频谱质心中位数 | 频域 |
| 4 | Q25 | 第一四分位数 | 频域 |
| 5 | Q75 | 第三四分位数 | 频域 |
| 6 | IQR | 四分位距 | 频域 |
| 7 | skew | 偏度（三阶矩） | 统计 |
| 8 | kurt | 峰度（四阶矩） | 统计 |
| 9 | sp.ent | 谱熵 | 频域 |
| 10 | mode | 频谱模值峰值频点 | 频域 |
| 11 | centroid | 频谱质心（原始值） | 频域 |
| 12 | meanfun | 平均基频 | F0 |
| 13 | minfun | 最低基频 | F0 |
| 14 | maxfun | 最高基频 | F0 |
| 15 | meandom | 平均主导频率 | 频域 |
| 16 | maxdom | 最大主导频率 | 频域 |
| 17 | dfrange | 主导频率动态范围 | 频域 |
| 18 | modindx | 调制指数 | F0 |

注：相比原始 20 特征，移除了 sfm（谱平坦度）和 mindom（最小主频），两者在特征重要性分析中贡献极低且对噪声敏感。

**关键洞察**：所有 18 个特征均起源于 STFT（或转化为 F0 后再统计），没有任何特征直接来自时域采样值。这体现了频域分析在语音信号处理中的核心地位——信号的"特征"往往在变换域中才变得可分。

### 9.2 XGBoost 分类器

XGBoost 是基于梯度提升树（Gradient Boosting）的集成学习算法，在 18 维频域特征空间中学习男女声音的非线性决策边界。模型参数：

| 参数 | 值 | 含义 |
|------|-----|------|
| max_depth | 3 | 限制树深度防止过拟合 |
| n_estimators | 100 | 弱学习器（决策树）数量 |
| learning_rate | 0.1 | 每棵树的贡献权重 |
| base_score | 0.5 | 初始预测分数 |
| objective | binary:logistic | 二分类交叉熵损失 |

### 9.3 模型性能

![混淆矩阵](report_figures/fig5_confusion_matrix.png)

**训练数据**：RAVDESS 数据集 1320 条语音（660 男 + 660 女），**演员独立划分**（Actor 1-22 训练，Actor 23-24 测试），避免同一演员的语音同时出现在训练/测试集中造成数据泄漏。

测试集（Actor 23-24，264 条）结果：

| 指标 | 值 |
|------|-----|
| Accuracy | 79.2% |
| 男声准确率 | 61.7% |
| 女声准确率 | 96.7% |

全量数据（Actor 1-24，1320 条）预测：准确率 95.5%（女 95.0%，男 95.9%，F1=95.5%）。

> Actor 23（男性）的 meandom（主导频率）为 873Hz，而训练集男性均值为 513Hz。录音条件差异造成的域偏移是男声准确率偏低的主因，而非模型设计缺陷。这本质上反映了训练数据多样性（仅 11 位男演员）的局限。

![特征重要性](report_figures/fig3_feature_importance.png)

**图3 分析**：特征重要性排名揭示了频域特征的均衡贡献——meanfun（平均基频，10.2%）排名第一，maxfun（最高基频，8.2%）、modindx（调制指数，7.3%）紧随其后。Top 3 特征合计占模型重要性的 25.7%，特征分布均衡，不依赖单一特征。

### 9.4 分段预测与男女声占比

对于长音频（$\ge 3$s），系统采用混合预测策略：
- **全局预测**：对完整音频提取特征，给出主要性别判断（匹配训练流程）
- **分段投票**：2.5s 窗口、0.8s 步长滑动，每段独立预测，统计男女声比例
- 置信度 <55% 的段标记为 unknown，不计入统计

该方法在文件上传测试中显示男女声占比，帮助用户了解长音频中的性别分布。

---

## 十、实验讨论与局限性

### 10.1 手机扬声器回放的域偏移问题

实验中观察到：手机播放的录音识别准确率显著低于直接人声。原因分析：

1. **低频截止**：手机扬声器通常无法有效重放 400 Hz 以下的频率，导致男性基频（85-180 Hz）完全丢失
2. **频响染色**：扬声器的非平坦频率响应在频谱包络上叠加了与说话人无关的"设备特征"
3. **域偏移**：训练数据全部来自麦克风直接录音，而扬声器回放样本的频谱分布发生了系统性偏移

这是一个典型的信号通过非理想 LTI 系统后特征改变的案例：扬声器相当于一个带通滤波器，改变了输入信号的频谱，使下游分类器失效。

### 10.2 非正常说话的局限

唱歌、假声、吼叫等非正常发声模式会导致 F0 超出训练数据分布范围（如假声男高音 F0 可高达 400+ Hz），被误判为女声。这是因为模型学到的"低 F0 = 男性"规则在此类情形下不再成立。

### 10.3 变声效果局限

男→女变声成功率约 50-60%（而女→男接近 100%）。根本原因：
1. **频谱特征不可逆**：某些极端男性化频谱特征（如极低频谱熵 sp.ent <0.5）超出了信号处理的可修改范围
2. **训练数据多样性的限制**：模型仅见过 RAVDESS 北美英语演讲，未学习到"变声后的合成语音"的分布
3. **F0 与频谱特征耦合**：提升 F0 到女性范围（200+ Hz）后，频谱修复（共振峰移位）无法充分改变所有频域特征
4. 女→男方向更简单：降低 F0 + 削高频是信息减损操作，天然保持信号稳定性

### 10.4 实时性与精度的平衡

0.8 秒的预测间隔在"实时响应"和"累积足够信息"之间做了折中。更短的窗口可能导致 VAD 不足和信息不充分，更长的窗口会增加响应延迟。

---

## 十一、总结

本项目以"声音性别识别与变声"为载体，串联了信号与系统课程的主要知识点：

| 课程知识点 | 本项目对应实现 |
|-----------|--------------|
| 连续/离散时间信号 | 22kHz 采样、93ms 块处理 |
| **采样定理 (Nyquist)** | 频率归一化、WAV 量化编码 |
| **傅里叶变换 / STFT** | 语谱图、频谱质心、FFT 共轭对称性 |
| **滤波 (卷积/频率响应)** | FIR 平滑、中值滤波、频谱倾斜 |
| **信号能量与功率** | RMS 能量、VAD 自适应阈值检测 |
| **随机信号与统计处理** | 谱熵、偏度、峰度、分位数 |
| **源-滤波器模型** | WORLD 三参数分析-修改-合成闭环 |

**四个突出亮点**：
1. **STFT + 18 个频域特征**：采样 → 加窗 → DFT → 统计聚合的完整频域分析链，体现了信号从时域到频域再到特征空间的逐级抽象
2. **WORLD 分析-修改-合成闭环**：源（F0）+ 滤波器（SP）+ 噪声（AP）的分离与独立操作，是源-滤波器模型最完整的工程实现
3. **基于共振峰检测的局部频域拉伸**：创新性地在频谱包络上检测 F1-F4 共振峰并做局部 warping，相比全局频率轴拉伸更精准地模拟声道共振特性的性别差异
4. **多速率实时处理架构**：22kHz 采集 → 10Hz 图表 → 1.25Hz 预测的三层时间尺度，体现了实时信号处理系统的时间层级设计

---

## 参考文献

[1] Alan V. Oppenheim, Alan S. Willsky. *Signals and Systems* (2nd Edition). Prentice Hall, 1996.

[2] M. Morise, F. Yokomori, K. Ozawa. WORLD: A Vocoder-Based High-Quality Speech Synthesis System for Real-Time Applications. *IEICE Transactions on Information and Systems*, 2016.

[3] A. de Cheveigné, H. Kawahara. YIN, a fundamental frequency estimator for speech and music. *The Journal of the Acoustical Society of America*, 2002.

[4] Chen T, Guestrin C. XGBoost: A Scalable Tree Boosting System. *KDD*, 2016.

[5] McFee B, et al. librosa: Audio and Music Signal Analysis in Python. *SciPy*, 2015.

[6] RAVDESS (Ryerson Audio-Visual Database of Emotional Speech and Song). https://zenodo.org/record/1188976

---

*附录：所有图表均由 `gen_report_figures.py` 从项目实际代码和数据中生成，图表源文件位于 `report_figures/` 目录下。模型基于 RAVDESS 数据集重训（1320 条语音，演员独立划分 Actor 1-22/23-24）。*
